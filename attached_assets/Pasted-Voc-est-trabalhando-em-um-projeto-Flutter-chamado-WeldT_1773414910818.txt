Você está trabalhando em um projeto Flutter chamado **WeldTrace**.
Seu primeiro objetivo é **restaurar a estabilidade do projeto** antes de qualquer nova funcionalidade.

Execute as tarefas abaixo **exatamente nesta ordem**.

---

# 1 — Preparar ambiente

Entre na pasta do projeto Flutter:

flutter_app

Execute:

flutter pub get

Depois execute:

flutter pub run build_runner build --delete-conflicting-outputs

Se houver erros de arquivos travados (Windows):

* finalize processos java e gradle
* execute novamente o build_runner.

---

# 2 — Corrigir banco Drift

Abra:

lib/data/local/database/app_database.dart

Garanta que as seguintes tabelas estejam declaradas no banco:

ProjectsTable
MachinesTable
WeldsTable
WeldStepsTable
SensorLogsTable
WeldingStandardsTable
WeldingParametersTable

Se **WeldingStandardsTable** ou **WeldingParametersTable** não existirem, crie.

### WeldingStandardsTable

Colunas:

id TEXT primary key
name TEXT
description TEXT
weld_type TEXT
is_active BOOLEAN
last_synced_at DATETIME nullable

### WeldingParametersTable

Colunas:

id TEXT primary key
standard_id TEXT
pipe_material TEXT
pipe_diameter_mm REAL
sdr_rating TEXT
heating_time_s INTEGER nullable
changeover_time_max_s INTEGER nullable
cooling_time_s INTEGER nullable
fusion_pressure_bar REAL nullable
fusion_pressure_min_bar REAL nullable
fusion_pressure_max_bar REAL nullable
electrofusion_voltage_v REAL nullable
is_active BOOLEAN
sync_status TEXT
last_synced_at DATETIME nullable

---

# 3 — Corrigir tabela Machines

Na tabela Machines garantir coluna:

hydraulicCylinderAreaMm2 REAL nullable

Se não existir, adicionar migration.

---

# 4 — Atualizar schema

Incrementar schemaVersion do banco.

Adicionar migration se necessário.

---

# 5 — Regenerar Drift

Executar novamente:

flutter pub run build_runner build --delete-conflicting-outputs

Garantir que:

app_database.g.dart
projects_dao.g.dart
machines_dao.g.dart
welds_dao.g.dart
sensor_logs_dao.g.dart
welding_parameters_dao.g.dart

sejam gerados corretamente.

---

# 6 — Corrigir DAO welding_parameters_dao.dart

Garantir métodos:

Future<List<double>> getAvailableDiameters(
String standardId,
String pipeMaterial
)

Future<List<String>> getAvailableSdrRatings(
String standardId,
String pipeMaterial,
double diameter
)

Future<WeldingParametersTableData?> lookupParameters(
String standardId,
String pipeMaterial,
double diameter,
String sdrRating
)

Future<void> upsert(
WeldingParametersTableCompanion row
)

Implementar upsert com:

insertOnConflictUpdate

---

# 7 — Rodar análise

Executar:

flutter analyze

Corrigir erros simples de tipo e import.

---

# 8 — Commit

Criar commit:

drift: repair schema and dao stability

Descrição:

Restores database schema integrity and regenerates Drift files.
Adds WeldingStandards and WeldingParameters tables and fixes DAO methods.
