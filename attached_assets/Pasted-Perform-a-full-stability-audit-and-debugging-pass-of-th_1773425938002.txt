Perform a full stability audit and debugging pass of the WeldTrace project.

Goal: improve robustness, prevent runtime issues, and validate the welding traceability system before deployment.

Do not change the architecture.
Focus on validation, guards, and automated testing.

---

# 1 — Verify welding trace recorder sampling

Check WeldTraceRecorder.record().

Ensure sampling frequency does not exceed 1 sample per second.

Implement guard:

if (lastSampleTime != null) {
if (DateTime.now().difference(lastSampleTime!).inMilliseconds < 1000) {
return;
}
}

Store lastSampleTime after recording.

This prevents extremely large curves.

---

# 2 — Add maximum curve size protection

Inside WeldTraceRecorder:

const int maxPoints = 5000;

If points.length >= maxPoints:
stop recording additional samples.

Add comment explaining protection against runaway recording.

---

# 3 — Ensure deterministic signature generation

Inside WeldTraceSignature.generate():

Before serialization:

sort curve points by timeSeconds.

Example:

curve.sort((a,b) => a.timeSeconds.compareTo(b.timeSeconds));

Then build payload using JSON:

payload = jsonEncode({
"machineId": machineId,
"diameter": pipeDiameter,
"material": material,
"sdr": sdr,
"curve": curve,
"timestamp": timestamp.toIso8601String()
})

Compute SHA256 hash of payload.

---

# 4 — Extend weld signature coverage

Add welding parameters to signature payload:

fusionPressure
heatingTime
coolingTime
beadHeight

Ensure the signature changes if welding parameters change.

---

# 5 — Validate curve data before saving

Before storing traceCurveJson:

Ensure:

curve.length >= 2

If less than 2 samples:

still allow saving but mark weldTraceQuality = "LOW_SAMPLE_COUNT"

Add optional field to WeldsTable:

traceQuality TEXT

Migration required.

---

# 6 — Improve PDF report robustness

Inside WeldReportGenerator:

Add guards:

If curve is empty:

generate minimal report without chart.

Add statistics:

durationSeconds
maxPressure
averagePressure

Display statistics in PDF summary table.

Ensure PDF generation never throws fatal exception.

Wrap chart rendering in try/catch.

---

# 7 — Verify recorder integration

Audit weld_workflow_engine.dart.

Ensure recorder lifecycle:

recorder.start() only once

recorder.record() inside sensor loop

recorder.export() only during completeWeld()

Add null safety checks.

---

# 8 — Add QR code to welding report

Add dependency:

qr_flutter

In PDF report:

generate QR code containing:

weldSignature

Embed QR code image into PDF.

---

# 9 — Extend unit tests

Update:

test/unit/weld_trace_test.dart

Add tests:

sampling guard
max curve size
signature deterministic ordering
signature parameter sensitivity
QR code generation
large curve export (5000 points)

Ensure all tests pass.

---

# 10 — Perform automated validation

Run:

flutter pub get

flutter pub run build_runner build --delete-conflicting-outputs

flutter analyze

flutter test

Fix all warnings and errors.

---

# 11 — Run static checks

Search for:

unused imports
dead code
unreachable branches

Remove them.

---

# 12 — Final verification

Confirm these components compile and run correctly:

WeldTraceRecorder
WeldTraceSignature
WeldReportGenerator
weld_workflow_engine
database migrations

Ensure no runtime exceptions occur.

---

# 13 — Commit

stability: welding trace system hardening and automated validation

Description:

Improves recorder sampling, signature determinism, curve validation, PDF robustness, and adds QR code traceability.
