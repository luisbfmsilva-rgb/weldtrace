Perform a full audit of the welding calculation engine and apply a refinement to the physics model.

Do not rewrite the architecture.
Only verify correctness and improve the interfacial contact area model.

---

# 1 — Verify generator pipeline

Confirm the full data flow works correctly:

fallback generator
→ WeldingParametersTableCompanion
→ companionToRecord()
→ WeldingParametersTableData
→ buildFromRecord()
→ WeldingTable
→ PhaseParameters used by the workflow

If any step is missing or bypassed, fix it.

The notifier must follow this order:

generateFallbackForStandard()
companionToRecord()
buildFromRecord()

---

# 2 — Verify machine pressure calculation

Check that all three standards use the same pressure conversion logic.

Required sequence:

contactAreaMm2
→ fusionForceN
→ machinePressureBar
→ drag compensation

Formula must be:

forceN =
fusionPressureBar × 100000 × contactAreaMm2 / 1e6

machinePressureBar =
forceN / hydraulicCylinderAreaMm2

gaugePressureBar =
max(0, machinePressureBar − dragPressureBar)

---

# 3 — Verify contact area model

Current implementation:

contactArea =
π × pipeDiameter × beadWidth

This assumes the bead covers the full circumference.

Verify that beadWidth is calculated from wall thickness.

---

# 4 — Improve contact area model

Implement a more realistic molten bead contact area.

Instead of using full circumference, estimate bead contact band width.

Define:

beadContactFactor

Default value:

0.65

Effective circumference:

effectiveCircumference =
π × pipeDiameter × beadContactFactor

Then compute:

contactAreaMm2 =
effectiveCircumference × beadWidth

Add comment:

// effective molten bead contact band
// reduces full circumference assumption

---

# 5 — Apply consistently to all standards

Update:

dvs_2207.dart
iso_21307.dart
astm_f2620.dart

Replace previous contactArea calculation with the improved version.

---

# 6 — Add validation guard

Ensure contact area never becomes unrealistic.

Clamp:

contactAreaMm2 =
contactAreaMm2.clamp(100, 10000000)

Add comment explaining numeric guard.

---

# 7 — Extend generator tests

Update test suite:

test/unit/welding_table_generator_test.dart

Add tests verifying:

contact area calculation
machine pressure scaling with cylinder area
drag pressure compensation
large diameter pipes (315+ mm)

Ensure machine pressure increases when:

hydraulicCylinderArea decreases

---

# 8 — Add debug helper

Inside WeldingTableGenerator add optional debug output:

debugCalculation()

Return structure:

{
wallThickness
beadWidth
contactArea
fusionForce
machinePressure
}

This is useful for engineering verification.

---

# 9 — Run validation

flutter pub get

flutter pub run build_runner build --delete-conflicting-outputs

flutter analyze

flutter test

---

# 10 — Commit

physics: refine molten bead contact area model

Description:

Audits welding calculation pipeline and introduces improved molten bead contact area estimation.

Improves machine pressure calculation accuracy for large-diameter pipes while maintaining compatibility with existing APIs.
